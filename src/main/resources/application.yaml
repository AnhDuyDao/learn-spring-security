spring:
  application:
    name: SPRING-BOOT-SHOP

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/springboot_shop
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update